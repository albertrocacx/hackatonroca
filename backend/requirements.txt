fastapi==0.138.2
uvicorn[standard]==0.32.1
numpy>=2.0
model2vec>=0.3
anthropic>=0.115           # chat IA sobre resultados vía Azure AI Foundry (necesita CLAUDE_API_KEY)
httpx>=0.27                # diseña tu baño: gpt-image + descarga de fotos de producto (design.py)
openai>=1.40               # cliente Azure OpenAI (embeddings) para /search semántico
azure-search-documents>=11.5  # búsqueda vectorial/híbrida en Azure AI Search
azure-core>=1.30
python-dotenv>=1.0         # carga backend/.env (credenciales Azure fuera del código)
azure-storage-blob>=12.19  # OCR: descarga PDFs y sube .md a Blob Storage (ocr_pdf_blob.py)
pymupdf>=1.24              # OCR: renderiza cada página del PDF a imagen (ocr_pdf_blob.py)
python-multipart>=0.0.9    # subida multipart de fotos en /search/image
requests>=2.31             # cliente del endpoint DINO (image_search.py)
